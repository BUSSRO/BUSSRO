<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:background="@color/background"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="10dp"
        android:paddingBottom="10dp">

        <TextView
            android:id="@+id/txt_list_item_stSrch"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            tools:text="왕십리역 버스정류장"
            android:textColor="@color/white"
            android:textSize="18dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@id/iv_list_item_delete" />

        <ImageView
            android:id="@+id/iv_list_item_delete"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_baseline_close_24"
            app:layout_constraintTop_toTopOf="@id/txt_list_item_stSrch"
            app:layout_constraintBottom_toBottomOf="@id/txt_list_item_stSrch"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/txt_list_item_stSrch"
            app:tint="@color/light_gray"
            android:padding="3dp" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>