<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bussro">
    <!-- 권한 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BUSSRO"
        android:usesCleartextTraffic="true">

        <activity android:name=".view.sign.SignActivity"
            android:label="전광판 화면입니다. 버스기사에게 보여주세요."
            android:screenOrientation="landscape"/>
        <activity
            android:name=".view.busstop.NearbyBusStopActivity"
            android:label="" />
        <activity
            android:name=".view.settings.LicenseActivity"
            android:label="" />
        <activity
            android:name=".view.settings.SettingsActivity"
            android:label="@string/settings_activity_label"
            android:theme="@style/PreferenceScreen" />
        <activity
            android:name=".view.businfo.BusInfoActivity"
            android:label="" />
        <activity
            android:name=".view.findstation.FindStationActivity"
            android:label="" />
        <activity
            android:name=".view.buslist.BusListActivity"
            android:label="" />
        <activity
            android:name=".view.main.MainActivity"
            android:label="">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>